---
'@tanstack/angular-query': minor
---

Angular 20 update, SSR support (hydration controls and TransferState), and lazy signal initialization for the Angular adapter.

Reduce production bundle overhead with removable result-field enumeration, a tuple return from the internal base-query helper, and development guards around query and mutation injection-context assertions.
