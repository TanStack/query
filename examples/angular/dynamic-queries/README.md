# TanStack Query Angular dynamic queries example

This example keeps a list of items in an Angular signal and creates one
`injectQueries` entry for each item. Add and remove items to see the query list
and its rendered results update together.

To run this example:

- `pnpm install`
- `pnpm start`
