---
'@tanstack/react-query': patch
---

feat(react-query): declare `@types/react` as an optional peer dependency so the shipped declaration files resolve React's types from the consumer's installation
