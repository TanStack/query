name: Labeler

on:
  pull_request_target:

jobs:
  triage:
    name: Triage
    runs-on: ubuntu-latest
    steps:
      - name: Run Labeler
        uses: actions/labeler@v5
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
